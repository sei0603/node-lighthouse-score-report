# node-lighthouse-score-report

## How to use

collect the lighthouse report from the url

```
$ npm run report ${saveKey} ${url}
```

show the score

```
$ npm run score ${saveKey}
```

compare the score

```
$ npm run score ${beforeKey} ${afterKey}
```
